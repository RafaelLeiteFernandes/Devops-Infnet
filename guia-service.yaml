apiVersion: v1
kind: Service
metadata:
  name: guia-infnet
spec:
  type: LoadBalancer
  selector:
    app: guia-infnet
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
